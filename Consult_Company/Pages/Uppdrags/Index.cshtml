@page
@model Consult_Company.Pages.Uppdrags.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Uppdrag[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Uppdrag[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Uppdrag[0].StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Uppdrag[0].EndDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Uppdrag[0].Priority)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Uppdrag[0].TaskRoles)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Uppdrag) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Priority)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TaskRoles)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.IdTask">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.IdTask">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.IdTask">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
